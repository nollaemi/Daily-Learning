## 강원도에 위치한 생산공장 목록 출력하기
SELECT FACTORY_ID, FACTORY_NAME, ADDRESS
FROM FOOD_FACTORY
WHERE ADDRESS LIKE "%강원도%"
ORDER BY FACTORY_ID

### 개념학습
특정 문자 포함여부로 필터링
- 기본문법 : SELECT * FROM [테이블명] WHERE LIKE [조건]
- LIKE 조건: _(자리수 지정) or %(자리수 지정X) 사용
  1. LIKE "_A"
### 기억하기

