01-Retrieval
```text
│
├── Column Selection     # 컬럼 선택 (SELECT)
├── Row Selection        # 행 선택 (DISTINCT, LIMIT, OFFSET)
├── Sorting              # 정렬 (ORDER BY)
├── Combining Results    # 결과 합치기 (UNION, INTERSECT, EXCEPT)
└── Data Source          # 조회 대상 (FROM, VIEW, TABLE)
